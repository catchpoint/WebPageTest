#!/bin/sh
basedir=`dirname "$0"`

case `uname` in
    *CYGWIN*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../jog/bin/jog" "$@"
  ret=$?
else 
  node  "$basedir/../jog/bin/jog" "$@"
  ret=$?
fi
exit $ret
